import { createSlice } from "@reduxjs/toolkit";
const initialState={

}


export const AuthSlice=createSlice(
    {
        name:"auth",
        initialState,
        reducers:{
            
        }

    }
)