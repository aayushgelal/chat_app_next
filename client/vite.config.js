module.exports = {
  // Enable Vite plugins
  root: './app',

  // Add the necessary dependencies to optimizeDeps.include
 
  // Configure Vite's server proxy if needed
  server: {
    proxy: {
        
    },
  },
  

};
